<%= render 'shared/notice_messages' %>

<table>
  <tr>
    <th><%= @post.titulo %></th>
    <td><%= @post.contenido %></td>
    <td><%= @post.veces_leida %></td>
  </tr>

</table>

<%= link_to "Editar", edit_admin_post_path(@post) %>
<%= link_to "Eliminar", admin_post_path(@post), method: :delete, data: { confirm: '¿Seguro de eliminar post?' }%>
<%= link_to "Regresar", admin_posts_path %>
